name: apache-livy
summary: REST API for HUE to connect to Apache Spark
maintainer: Big Data Team <bigdata@lists.ubuntu.com>
description: |
        LIVY
        livy
tags: ["bigdata", "hadoop", "apache"]
subordinate: true
requires:
  spark:
    interface: spark
    scope: container

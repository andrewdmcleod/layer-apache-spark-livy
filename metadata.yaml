name: apache-spark-livy
summary: REST API for HUE to connect to Apache Spark
maintainer: Big Data Team <bigdata@lists.ubuntu.com>
description: |
        LIVY
        livy
tags: ["bigdata", "hadoop", "apache"]
subordinate: true
provides:
  client:
    interface: livy 
requires:
  spark:
    interface: spark
    scope: container
